# =============================================================================
# S3Pool Configuration Example
# =============================================================================

# -----------------------------------------------------------------------------
# Required (both CLI and Proxy)
# -----------------------------------------------------------------------------
# Comma-separated list of S3 endpoints
S3_POOL=https://s3-1.example.com,https://s3-2.example.com,https://s3-3.example.com

# AWS credentials (or use S3_KEY/S3_SECRET as fallback)
AWS_ACCESS_KEY_ID=YOUR_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=YOUR_SECRET_ACCESS_KEY

# -----------------------------------------------------------------------------
# Optional - CLI
# -----------------------------------------------------------------------------
# AWS_REGION=us-east-1          # Default region (defaults to us-east-1)
# S3_BUCKET=my-bucket           # Default bucket (can specify per-command)

# -----------------------------------------------------------------------------
# Optional - Proxy
# -----------------------------------------------------------------------------
# PROXY_LISTEN=127.0.0.1:18080  # Proxy listen address
# LB_STRATEGY=round_robin       # round_robin, least_connections, power_of_two
# LB_HEALTH_CHECK_INTERVAL=30   # Health check interval in seconds
# LB_REQUEST_TIMEOUT=30         # Request timeout in seconds
# LB_MAX_RETRIES=3              # Max retries per request

# -----------------------------------------------------------------------------
# Optional - Connection Pool
# -----------------------------------------------------------------------------
# POOL_MAX_CONNECTIONS=100      # Max connections per backend
# POOL_MIN_CONNECTIONS=10       # Min connections to keep warm
# POOL_MAX_IDLE_TIME=90         # Max idle time (seconds) before closing
# POOL_CONNECT_TIMEOUT=5        # Connection timeout (seconds) - fast failure detection
