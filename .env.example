# S3 Configuration
# Comma-separated list of S3 endpoints
S3_POOL=https://s3-1.example.com,https://s3-2.example.com,https://s3-3.example.com

# AWS credentials
S3_KEY=YOUR_ACCESS_KEY_ID
S3_SECRET=YOUR_SECRET_ACCESS_KEY

# S3 settings
AWS_REGION=us-east-1
S3_BUCKET=my-bucket

# Optional: Load balancer algorithm (round-robin, least-connections, power-of-two)
S3POOL_LB_ALGORITHM=power-of-two

# Optional: Max connections per backend (default: 100)
S3POOL_MAX_CONNECTIONS=100

# Optional: Health check interval in seconds (default: 5)
S3POOL_HEALTH_CHECK_INTERVAL=5
